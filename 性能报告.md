# MyDB 性能报告

> 本报告的对比在 **Docker 容器** 中进行，MyDB 与 MySQL 运行在**完全相同的 CPU 与内存限制**下（各 2 vCPU、2 GiB 内存），保证公平可复现。双方持久化级别一致：MyDB 每组一次 WAL `sync_data()`；MySQL `innodb_flush_log_at_trx_commit=1` + `sync_binlog=1`。每项取 1 次采样中位数。

> 基准工具为 `mydb-bench`，双方执行完全相同的 `ENGINE=InnoDB` 业务 DDL/DML；仅创建并删除唯一命名的 `mydb_game_bench_*` 临时库。性能阶段不预热，默认单样本并要求在 60 秒内完成。

---

## 测试方法

| 场景 | actors | tables | transaction_size | writes_per_actor | 说明 |
|------|--------|--------|------------------|------------------|------|
| 单表写 | 1 | 1 | 1 | 200 | 单连接单行事务，每次 COMMIT 持久化 |
| 并发 P99 | 4 | 4 | 5 | 80 | 4 并发写、10 点查/actor |
| 并发吞吐 | 4 | 4 | 1 | 200 | 800 次单行事务，测真实 group commit |

## 当前实测（Docker 受控）

### Git Revision: `878ec81`
### 测试日期: 2026-08-14
### 性能阶段耗时: 22.4s / 60s（无预热）
### 环境: Docker Desktop，linux/amd64；MyDB 容器 2 vCPU / 2g，MySQL 容器 2 vCPU / 2g
### MySQL: 8.4.11 — MySQL Community Server - GPL — `innodb_flush_log_at_trx_commit=1`, `sync_binlog=1`, `transaction_isolation=REPEATABLE-READ`
### MyDB: `group_commit_window_us=250`，shard_count 自动 = 分配 CPU 数（2），checkpoint 每 1024 个已提交请求

| 场景 | MyDB | MySQL 8.4.11 | MyDB / MySQL |
|------|------|--------------|---------------|
| 单表写 | 205 ops/s | 80 ops/s | 2.58x |
| 4 actor / 4表 写 P99 | 28.5 ms | 57.4 ms | 2.01x（低更好） |
| 4 actor / 4表 吞吐 | 280 ops/s | 149 ops/s | 1.88x |
| 读 P50 | 334 μs | 141 μs | - |

## MyDB 观测

- write groups: n/a
- grouped requests: n/a
- checkpoints: n/a
- WAL sync total: n/a μs
- checkpoint total: n/a μs

## 性能原则

- 已确认提交必须由一次 `sync_data()` WAL durability boundary 覆盖；不以关闭持久化换吞吐。
- 默认 250μs Group Commit 窗口优先并发吞吐；`0` 是显式低延迟档，报告必须写明所用档位。
- 多核并行：MyDB 按表命名空间分片为独立 Leader/Follower 提交组，每组独立 WAL 与 fsync，吞吐随分配 CPU 数线性扩展。
- checkpoint 按已提交请求数触发，不与合批大小耦合；崩溃恢复、`flush_consistent`、shutdown 仍强制落盘。
- MySQL 比较必须同机（同容器宿主）、实际运行、相同负载，并记录 MySQL 版本与持久化设置；禁止硬编码历史比值。
